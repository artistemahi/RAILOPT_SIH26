# Internal prototype demo scope

The planned internal prototype flow is:

~~~
synthetic data
  -> risk / priority
  -> conflict detection
  -> CP-SAT optimization
  -> block plan
  -> what-if or emergency replan
~~~

This scope is illustrative only. The current repository implements none of these domain stages; it provides the isolated services and documentation needed to begin them safely.
